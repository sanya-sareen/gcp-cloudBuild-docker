steps:
#  Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args: [
            'build',
            '-t',
            'us-central1-docker.pkg.dev/commanding-fact-455705-p5/python-repo:${SHORT_SHA}',
            '.'
          ]

# - # Docker push to Google Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push',  'us-central1-docker.pkg.dev/commanding-fact-455705-p5/python-repo:${SHORT_SHA}']
 